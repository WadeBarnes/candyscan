apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: indyscan-es
  labels:
    io.kompose.service: indyscan-es
    app.kubernetes.io/part-of: CANdyScan
    env: prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
