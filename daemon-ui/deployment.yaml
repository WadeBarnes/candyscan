apiVersion: apps/v1
kind: Deployment
metadata:
  name: candyscan-daemon-ui
  labels:
    name: candyscan-daemon-ui
    app: candyscan-daemon-ui
    app.kubernetes.io/part-of: CANdyScan
    env: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: candyscan-daemon-ui
  template:
    metadata:
      labels:
        name: candyscan-daemon-ui
        app: candyscan-daemon-ui
        app.kubernetes.io/part-of: CANdyScan
        env: prod
    spec:
      containers:
        - image: pstas/indyscan-daemon-ui:4.1.0
          name: candyscan-daemon-ui
          ports:
            - containerPort: 3710
          env:
            - name: CHOKIDAR_USEPOLLING
              value: "true"
            - name: PORT
              value: "3710"
            - name: PROXY_API_URL
              value: http://candyscan-daemon:3709
